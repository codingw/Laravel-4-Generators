@extends('layouts.scaffold')

@section('main')

<h1>
  {{Models}}
  <smaller>{{ link_to_route('{{models}}.create', 'add') }}</smaller>
</h1>

@if (${{models}}->count())
	<table class="table table-striped table-bordered">
		<thead>
			<tr>
				{{headings}}
			</tr>
		</thead>

		<tbody>
			@foreach (${{models}} as ${{model}})
				<tr id="{{model}}_{${{model}}->id}" class="{{model}}">
					{{fields}}
				</tr>
			@endforeach
		</tbody>
	</table>
@else
	There are no {{models}}
@endif

@stop
